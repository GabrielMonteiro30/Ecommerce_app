

<% if user_signed_in? %>
  <p>Cliente está logado</p>
<% else %>
  <p> Cliente não está logado / <%= current_user %> </p>
<% end %>

<% if admin_signed_in? %>
  <p>Administrador está logado / <%= current_admin %> </p>
<% else %>
  <p>Administrador não está logado</p>
<% end %>

<p/> 
